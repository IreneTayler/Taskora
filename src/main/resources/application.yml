spring:
  datasource:
    url: jdbc:h2:mem:orchestratordb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: nexus
    password: nexus123
    driver-class-name: org.h2.Driver

  flyway:
    enabled: false

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true
      path: /orchestrator-console